import { ClientsPageClient } from '../../../../components/admin/clients/ClientsPageClient';

export const metadata = { title: 'Clients — Gx-Portal' };

export default function ClientsPage() {
  return <ClientsPageClient />;
}
