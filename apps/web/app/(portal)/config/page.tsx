import { ConfigPageClient } from '../../../components/config/ConfigPageClient';

export const metadata = { title: 'Config — Gx-Portal' };

export default function ConfigPage() {
  return <ConfigPageClient />;
}
